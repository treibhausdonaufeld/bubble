import '@popperjs/core';
import * as bootstrap from 'bootstrap';
import $ from 'jquery';
import 'select2';

// Expose jQuery and Bootstrap to global window object
window.$ = $;
window.jQuery = $;
window.bootstrap = bootstrap;
